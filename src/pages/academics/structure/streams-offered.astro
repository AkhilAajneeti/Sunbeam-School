---
/**
 * ACADEMIC STRUCTURE · STREAMS OFFERED — audit §2B.
 *
 * ⚠ ROUTING AND A PHOTOGRAPH ONLY. The page is
 * components/academics/TopicPage — the same component the Assessment and
 * Teaching & Learning topics use, so the treatment cannot drift between the
 * four sets. The content lives in data/academicTopics.ts.
 */
import TopicPage from '../../../components/academics/TopicPage.astro';
import { structureTopics } from '../../../data/academicTopics';
import photo from '../../../assets/chem lab/DSC_1262 copy.jpg';

const topic = structureTopics.find((t) => t.id === 'streams-offered')!;
---

<TopicPage topic={topic} photo={photo} alt="A subject laboratory at Sunbeam School Ballia, benches laid out with apparatus." position="50% 50%" />
