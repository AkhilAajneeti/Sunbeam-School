---
/**
 * CAREER DEVELOPMENT & STUDENT SUCCESS · CAREER GUIDANCE — audit §2E.
 *
 * ⚠ ROUTING AND A PHOTOGRAPH ONLY. The page is
 * components/academics/TopicPage — the same component the Assessment and
 * Teaching & Learning topics use, so the treatment cannot drift between the
 * four sets. The content lives in data/academicTopics.ts.
 */
import TopicPage from '../../../components/academics/TopicPage.astro';
import { careerTopics } from '../../../data/academicTopics';
import photo from '../../../assets/library/DSC_1224 copy.jpg';

const topic = careerTopics.find((t) => t.id === 'career-guidance')!;
---

<TopicPage topic={topic} photo={photo} alt="The Nalanda Library at Sunbeam School Ballia, its shelves labelled by subject." position="50% 50%" />
