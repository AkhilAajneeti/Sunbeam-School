User-agent: *
Allow: /

# The not-yet-written sections generated by src/pages/[...slug].astro return a
# real 200 and carry <meta name="robots" content="noindex, follow">, so they are
# kept out of results by the page itself rather than by a rule here — a path
# rule would have to be edited every time one of them is finished.

Sitemap: https://sunbeamballia.edu.in/sitemap-index.xml
